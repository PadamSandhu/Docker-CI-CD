sudo: required
services:
  - docker

before_install:
  - docker build -t sandhp7/docker-react -f Dockerfile.dev .

script:
  - docker run sandhp7/docker-react npm run test -- --coverage
## Above code is just to check the test results
